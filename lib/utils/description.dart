import 'package:flutter/material.dart';

class DisInfo {
  String ManDisease; //location name for the ui
  // String? remedy; //time in that location
  String pic; //url to an asset flag icon
  //location url for api end point
  // true or false if daytime or not

  DisInfo({required this.ManDisease, required this.pic});
}
